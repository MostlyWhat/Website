---
import authorData from '../data/authors.json';

export interface Props {
  authorId: string;
}

const { authorId } = Astro.props;
const author = authorData[authorId];
---
<div class="author">
  <p>by <b>{author.name}</b> aka {author.aliases}</p>
</div>
